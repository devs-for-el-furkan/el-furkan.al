Prefer the smallest read/search that answers the question (targeted Grep, line-ranged Read) over dumping whole files, and don't re-read a file already in context unless it changed. Keep new code and docs comment-free unless a non-obvious WHY needs capturing. Periodically scan for redundant or unnecessary files — dead components, duplicate docs, leftover scratch files, unused exports. Verify with Grep for references/imports first: if you're confident something is unused, delete it and tell the user what you removed and why; if ambiguous, flag it instead of guessing.
